# Production profile — no demo seed data (DataInitializer is dev-only).
# AdminBootstrap still creates the default admin when the users table is empty.
retailpulse:
  seed:
    import-90day-sample: false
